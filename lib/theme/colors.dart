import 'package:flutter/material.dart';

class SolaColors {
  // Brand Backgrounds
  static const Color bgMain = Color(0xFFF7F5F0);          // Warm Cream/Off-white
  static const Color bgCard = Color(0xFFFFFFFF);          // Pure White
  static const Color bgCardAlt = Color(0xFFFAF8F5);       // Softer Cream

  // Sage Greens (Calming, Physical)
  static const Color sagePrimary = Color(0xFF768A7E);     // Muted Sage
  static const Color sageMedium = Color(0xFF95A79C);      // Mid Sage
  static const Color sageLight = Color(0xFFC2D3C9);       // Soft Pastel Sage
  static const Color sageAccent = Color(0xFFE8F0EB);      // Ultra-soft Green

  // Slate Blues (Cognitive, Grounding)
  static const Color slatePrimary = Color(0xFF5A737E);    // Slate Blue
  static const Color slateLight = Color(0xFF8499A3);      // Mid Slate
  static const Color slateAccent = Color(0xFFECF2F5);     // Soft Blue Highlight

  // Accents
  static const Color pinkAccent = Color(0xFFF4EAE6);      // Soft Pink (Reflection)
  static const Color coralSoft = Color(0xFFE0A99E);       // Soft warning / Crisis
  static const Color goldAccent = Color(0xFFF4D068);      // Gold (Constellation Stars)

  // Typography
  static const Color textPrimary = Color(0xFF2B332F);     // Deep Charcoal Green
  static const Color textSecondary = Color(0xFF5F6C65);   // Soft Charcoal
  static const Color textMuted = Color(0xFF8E9E95);       // Muted grey-green
}
